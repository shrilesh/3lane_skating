


# def blink_pin(pin, delay):
	
# 	while not pin_stop.is_set():
# 		GPIO.output(pin, GPIO.HIGH)
# 		time.sleep(delay)
# 		GPIO.output(pin, GPIO.LOW)
# 		time.sleep(delay)
		












# def blink(pin,delay):

# 	pin12_stop = threading.Event()
# 	pin12 = threading.Thread(target=blink_pin, args=(pin, delay))
# 	pin13_stop = threading.Event()
# 	pin13 = threading.Thread(target=blink_pin, args=(pin, delay))
# 	